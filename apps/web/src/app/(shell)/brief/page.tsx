"use client";

import { BriefEditor } from "@/components/campaign/BriefEditor";

/** The campaign editor, on whichever brief the shell has active. */
export default function BriefPage() {
  return <BriefEditor />;
}
