{
  "version": 1,
  "lane": "X30",
  "mutations": [
    {
      "file": "apps/web/src/components/campaign/BriefEditor.tsx",
      "before": "  const errors = useMemo(() => validateState(state, existingIds), [state, existingIds]);\n  const warnings = useMemo(() => validateWarnings(state), [state]);\n  // Not a boolean: the section that blocks is what the refusal needs to scroll to, and\n  // deriving it here keeps \"is it blocked\" and \"where\" from disagreeing. null = valid.\n  // PE1: the blocking section AND the keys that block it, from one derivation.\n  // The same argument the section's own comment makes — \"is it blocked\" and\n  // \"where\" must not disagree — applies with more force to \"where\" and \"which\n  // field\": two memos would each re-run `validateState` against the same state\n  // and could only ever agree by coincidence.\n  const blocked = useMemo(() => {\n    // D7: Save is blocked by structural invalidity only. A capability being off\n    // makes the draft unrunnable on this host, not unsavable — so the gating check\n    // runs the same validation with the capability unknown. The API parses saves in\n    // authoring mode; a motion brief must round-trip on a host without ffmpeg.\n    const structural = validateState({ ...state, capabilities: null }, existingIds);\n    // M1: the refusal bounces to the first failing *step* in the order the user\n    // walks, never the first bucket in validateState's key order — that order puts\n    // `policy` before `output`, so a variation draft failing both used to land the\n    // user on Variation Policy while the walk reaches Output first. Motion is not a\n    // step; it sorts at its host's position, where its panel lives.\n    const walk = sectionOrder(state.mode);\n    const walkIndex = (bucket: string) =>\n      walk.indexOf((bucket === MOTION_ERROR_KEY ? MOTION_HOST_SECTION : bucket) as SectionId);\n    const section =\n      Object.keys(structural)\n        .filter((bucket) => getTotalErrorCount({ [bucket]: structural[bucket] }) > 0)\n        .sort((a, b) => walkIndex(a) - walkIndex(b))[0] ?? null;\n    // Only the blocking bucket's keys: the refusal lands inside the section it\n    // names, never in a later one that also fails. No `?? {}` on the lookup —\n    // `section` came out of `Object.keys(structural)`, so it indexes something\n    // by construction, and a fallback there would be an unreachable branch\n    // dressed as caution.\n    return {\n      section,\n      fieldKeys: section === null ? EMPTY_FIELD_KEYS : new Set(Object.keys(structural[section])),\n    };\n  }, [state, existingIds]);\n  const blockedAt = blocked.section;",
      "after": "  const [errors, setErrors] = useState<Record<string, FieldErrors>>({});\n  const [warnings, setWarnings] = useState<Record<string, Record<string, string>>>({});\n  const [blocked, setBlocked] = useState<{\n    section: string | null;\n    fieldKeys: ReadonlySet<string>;\n  }>({ section: null, fieldKeys: EMPTY_FIELD_KEYS });\n  useEffect(() => {\n    setErrors(validateState(state, existingIds));\n    setWarnings(validateWarnings(state));\n    const structural = validateState({ ...state, capabilities: null }, existingIds);\n    const walk = sectionOrder(state.mode);\n    const walkIndex = (bucket: string) =>\n      walk.indexOf((bucket === MOTION_ERROR_KEY ? MOTION_HOST_SECTION : bucket) as SectionId);\n    const section =\n      Object.keys(structural)\n        .filter((bucket) => getTotalErrorCount({ [bucket]: structural[bucket] }) > 0)\n        .sort((a, b) => walkIndex(a) - walkIndex(b))[0] ?? null;\n    setBlocked({\n      section,\n      fieldKeys:\n        section === null ? EMPTY_FIELD_KEYS : new Set(Object.keys(structural[section])),\n    });\n  }, [state, existingIds]);\n  const blockedAt = blocked.section;",
      "because": "X30's fix folds the validate-on-change effect's own setState-driven commit into the render `dispatch` already scheduled, by deriving `errors`/`warnings`/`blockedAt` with `useMemo` instead of mirroring `state` into a `useState` triple written from a `[state, briefs]` effect. Reverting to the effect+setState shape reopens exactly the CI timing regression this lane closes: every dispatch pays a second full commit of the \"everything\"-presentation tree for no reason other than the mirror, on top of the dirty-flag effect's own commit — three commits per interaction instead of two. The html weight meter (HL5c/HL5f/HL5e) was disproven as the cause (0 `assembleHtml` calls across all four originally-timing-out tests, and the timing is unchanged at `origin/main~6`, before the meter existed) — this mutation is the actual regression surface the lane's fix addresses, not the meter's cache or key. (Re-anchored by ANCHORS: the recorded text was reformatted or moved, not changed — the mutation, its claim and its command are untouched.)\n\nRE-POINTED by ANCHORS after #494. Recorded `caught`, observed SURVIVING once this manifest could replay again — the claim never stopped being true, the guard carrying it stopped discriminating. #494 diagnosed the mechanism and wrote a guard that does.\n\nThe `-t` pattern deliberately omits the test title's parenthesised prefix/suffix. `vitest -t` is a REGEX, so `(3b)` and `(X30)` are capture groups rather than literal parentheses: the full title matches NOTHING, every test is skipped, vitest exits 0, and `mutate:verify` reads that as `survived`. My first re-point did exactly that and reported a false survivor. The pattern here was verified to select exactly one test before being written, which is the only safe way to author this field.\n\nRE-ANCHORED by PE1 (2026-09-19). PE1 renamed this memo from `blockedAt` to `blocked` and gave it a second field (the blocking bucket's error keys, so a refusal can land on the failing FIELD rather than the top of its section). The claim is untouched: it is still `useMemo` versus mirroring the same values into `useState` from a `[state, …]` effect, and the after-text still reopens exactly that second commit per dispatch.\n\nThe anchor was also NARROWED, and that is a fix rather than a convenience. The recorded region began at `const existingIds = useMemo(…)` and the after-text dropped that binding — but `existingIds` is read at three later sites in this component (`validationStands`, and the `setValidation` effect and its deps). Replaying the old pair would therefore have failed to COMPILE, and `mutate:verify` would have read the compile error as the mutation being caught: a green verdict that owed nothing to the timing claim it is supposed to be making. The region now starts at `const errors = useMemo(…)`, so `existingIds` survives the mutation and what reds is the claim itself.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "web",
        "apps/web/src/app/(shell)/brief/__tests__/brief-editor.test.tsx",
        "-t",
        "three keystrokes cost the editor three commits"
      ],
      "verdict": "caught"
    }
  ],
  "note": "No second mutation: the brief's suggested mutation (b) — drop a slot from the html weight key — presumes the fix touches `htmlWeightKey`. X30 disproved the meter (0 `assembleHtml` calls in all four originally-timing-out tests; identical timing on origin/main~6, before the meter existed) and never touched `derive.ts`'s cache or key, so that mutation does not apply here and is not manufactured to fill the slot."
}
