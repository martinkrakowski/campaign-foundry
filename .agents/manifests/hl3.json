{
  "version": 1,
  "lane": "HL3",
  "mutations": [
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "  logo: drawLogo,\n  html: drawHtml,\n};",
      "after": "  logo: drawLogo,\n};",
      "because": "The LAYER_DRAWERS dispatch table must register the html layer drawer so that templates with an html layer can render their backup image on the canvas rather than throwing.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "      `NodeCanvasCompositor: layer kind \"${layer.kind}\" has no drawer in this compositor — it draws image, video, shade, accent, static-text, animated-text, logo and html only`,",
      "after": "      `NodeCanvasCompositor: layer kind \"${layer.kind}\" has no drawer in this compositor — it draws image, video, shade, accent, static-text, animated-text and logo only`,",
      "because": "The error message in drawLayer must list html among the supported layer kinds when an unknown kind is encountered, keeping diagnostic messages accurate for unsupported layers.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "    if (anchor === undefined) {\n      if (!prepared.layers.some((layer) => layer.kind === \"html\")) {\n        throw new Error(\n          \"NodeCanvasCompositor: the logo layer snaps to the text block, but there is no text layer in the template at all\",\n        );\n      }\n    }",
      "after": "    if (anchor === undefined) {\n      throw new Error(\n        \"NodeCanvasCompositor: the logo layer snaps to the text block, but there is no text layer in the template at all\",\n      );\n    }",
      "because": "drawLogo must allow rendering when anchor is undefined if the template has an html layer, allowing canonical image-html templates with a logo to render without requiring a static-text layer.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CampaignOrchestration/src/application/use-cases/GenerateCampaignUseCase.use-case.ts",
      "before": "    const copyCompat = this.validateCopyTemplateCompatibility(brief);\n    if (!copyCompat.success) return copyCompat;\n\n    return ok(true);",
      "after": "    const copyCompat = this.validateCopyTemplateCompatibility(brief);\n    if (!copyCompat.success) return copyCompat;\n\n    if (brief.template.creativeType === \"image-html\") {\n      return err(\n        new Error(\n          'Campaign brief template creative type \"image-html\" is not supported: HTML ad delivery is deferred until HL4/HL5 (#331)',\n        ),\n      );\n    }\n\n    return ok(true);",
      "because": "GenerateCampaignUseCase must not reject image-html creative types at the boundary guard now that the compositor has an HTML layer drawer to produce backup renditions.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CampaignOrchestration/src/application/use-cases/__tests__/GenerateCampaignUseCase.use-case.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "      case \"button\": {\n        const radius = Math.min(8, boxH / 2, boxW / 2);\n        ctx.save();\n        // Clip to the rounded shape the button is filled with: the markup's\n        // `border-radius` + `overflow: hidden` clips its label to that shape too.\n        ctx.beginPath();\n        ctx.roundRect(boxX, boxY, boxW, boxH, radius);\n        ctx.clip();\n        ctx.fillStyle = prepared.brandColor;\n        ctx.beginPath();\n        ctx.roundRect(boxX, boxY, boxW, boxH, radius);\n        ctx.fill();\n\n        ctx.fillStyle = \"#ffffff\";\n        ctx.textAlign = \"center\";\n        ctx.textBaseline = \"middle\";\n        const fontSize = htmlButtonFontSize(boxH, scaleBasis(prepared.canvas, width, height));\n        // HL5e: the element's own font, resolved by the one function\n        // `assembleHtml` also calls — the button's override if it names one,\n        // the brief's resolved weight/family otherwise.\n        const font = htmlElementFont(element, prepared);\n        ctx.font = `${font.fontWeight} ${fontSize}px ${font.fontFamily}, sans-serif`;\n        ctx.fillText(element.text!, boxX + boxW / 2, boxY + boxH / 2);\n        ctx.restore();\n        break;\n      }",
      "after": "      case \"button\": {\n        break;\n      }",
      "because": "The drawHtml drawer must handle button elements by rendering the rounded button background with brandColor and drawing the button text label onto the canvas. (Re-anchored by ANCHORS: X10 (#391) added the rounded clip and HL5e resolved the button's font and font size through named helpers, so the case block grew around the claim; The semantic edit is the same one; the after-text is that edit re-expressed against the current shape, not a new claim.)",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "      case \"image\": {\n        ctx.save();\n        ctx.drawImage(prepared.background, boxX, boxY, boxW, boxH);\n        ctx.restore();\n        break;\n      }",
      "after": "      case \"image\": {\n        break;\n      }",
      "because": "The drawHtml drawer must render image elements using the prepared background asset into the element's bounding box frame.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    },
    {
      "file": "packages/CreativeGeneration/src/infrastructure/adapters/NodeCanvasCompositor.ts",
      "before": "  const { ctx, prepared, layer } = c;\n  if (layer.elements === undefined || layer.elements.length === 0) {\n    return;\n  }\n  const { width, height } = prepared;\n  for (const element of layer.elements) {",
      "after": "  const { ctx, prepared } = c;\n  const htmlLayer = prepared.layers.find((layer) => layer.kind === \"html\");\n  if (htmlLayer === undefined || htmlLayer.elements === undefined || htmlLayer.elements.length === 0) {\n    return;\n  }\n  const { width, height } = prepared;\n  for (const element of htmlLayer.elements) {",
      "because": "drawHtml must read its elements from the dispatched layer on the draw context — a template may carry two html layers, and hunting prepared.layers for \"the\" html layer paints the first list twice and never paints the second.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CreativeGeneration/src/infrastructure/adapters/__tests__/NodeCanvasCompositor.layer-order.test.ts"
      ],
      "verdict": "caught"
    }
  ]
}
