{
  "version": 1,
  "lane": "AF1",
  "mutations": [
    {
      "file": "packages/CampaignOrchestration/src/domain/value-objects/markup-assembler.ts",
      "before": "    if (layer.kind === \"static-text\" && layer.link === true) {\n      const box = `${layerBoxStyle(layer, options.canvas, width, height)} background: transparent; border: none; padding: 0; margin: 0;`;\n      const nav = hasClick ? CLICK_OPEN : \"\";\n      const hiddenSpan = `<span style=\"position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0, 0, 0, 0);\">${copy}</span>`;\n      layerMarkup.push(`<button type=\"button\" style=\"${box}\"${nav}>${hiddenSpan}</button>`);\n    }",
      "after": "    if (layer.kind === \"static-text\") {\n      const box = `${layerBoxStyle(layer, options.canvas, width, height)} background: transparent; border: none; padding: 0; margin: 0;`;\n      if (layer.link === true) {\n        const nav = hasClick ? CLICK_OPEN : \"\";\n        const hiddenSpan = `<span style=\"position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0, 0, 0, 0);\">${copy}</span>`;\n        layerMarkup.push(`<button type=\"button\" style=\"${box}\"${nav}>${hiddenSpan}</button>`);\n      } else {\n        layerMarkup.push(`<p style=\"${box}\">${copy}</p>`);\n      }\n    }",
      "because": "unlinked static-text must emit no paragraph because the fallback raster already paints the headline (D164).",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "node",
        "packages/CampaignOrchestration/src/domain/value-objects/__tests__/markup-assembler.test.ts",
        "-t",
        "unlinked static-text emits no paragraph"
      ],
      "verdict": "caught"
    }
  ]
}
