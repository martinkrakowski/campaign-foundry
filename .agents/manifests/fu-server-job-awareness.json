{
  "version": 1,
  "lane": "FU-server-job-awareness",
  "mutations": [
    {
      "file": "apps/web/src/lib/run-context.tsx",
      "before": "        void adoptJob(startBrief, jobId);",
      "after": "          void startBrief; void jobId;",
      "because": "skipping the adoption call after restoring a brief prevents the web client from adopting an in-flight background run",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "web",
        "apps/web/src/lib/__tests__/run-context.test.tsx",
        "-t",
        "reload adopts a running job"
      ],
      "verdict": "caught"
    },
    {
      "file": "apps/web/src/lib/run-context.tsx",
      "before": "  const adoptJob = useCallback(async (target: CampaignBrief, jobId: string) => {\n    setLoading(true);",
      "after": "  const adoptJob = useCallback(async (target: CampaignBrief, jobId: string) => {",
      "because": "failing to set loading when adopting a running job lets the UI show settled controls while background work is still polling",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "web",
        "apps/web/src/lib/__tests__/run-context.test.tsx",
        "-t",
        "reload adopts a running job"
      ],
      "verdict": "caught"
    }
  ]
}
