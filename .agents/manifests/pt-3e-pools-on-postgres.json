{
  "version": 1,
  "lane": "PT-3e",
  "mutations": [
    {
      "file": "apps/api/server/lib/ports/pg-pool-store.ts",
      "before": "       where org_id = $1 and campaign_id = $2 and revision = $5",
      "after": "       where org_id = $1 and campaign_id = $2 -- mutant: revision condition dropped",
      "because": "dropping the revision condition from the compare-and-swap UPDATE makes writePool's expectedRevision check unconditional: a stale-revision write would succeed instead of conflicting, matching FsPoolStore's ECONFLICT guard only by coincidence.",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "api",
        "apps/api/server/lib/ports/__tests__/pg-pool-store.test.ts",
        "-t",
        "a stale expected revision is refused"
      ],
      "verdict": "caught"
    }
  ]
}
