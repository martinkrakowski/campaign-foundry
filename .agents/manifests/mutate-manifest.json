{
  "version": 1,
  "lane": "mutation-manifest",
  "mutations": [
    {
      "file": "tools/mutate-manifest/lib/replay.ts",
      "before": "status: result.verdict === mutation.verdict ? \"verified\" : \"mismatch\",",
      "after": "status: \"verified\",",
      "because": "If every replayed mutation reports as verified, the tool certifies any claim a lane makes - including the false ones it exists to catch. A green check on an unverified claim is worse than no check.",
      "command": ["yarn", "vitest", "run", "tools/mutate-manifest"],
      "verdict": "caught"
    },
    {
      "file": "tools/mutate-manifest/lib/manifest.ts",
      "before": "if (verdict === \"survived\") {",
      "after": "if (false as boolean) {",
      "because": "A manifest recording a survived mutation is a lane shipping a test it knows does not catch. Accepting one would let the record say the opposite of what happened.",
      "command": ["yarn", "vitest", "run", "tools/mutate-manifest"],
      "verdict": "caught"
    }
  ]
}
