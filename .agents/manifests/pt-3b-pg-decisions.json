{
  "version": 1,
  "lane": "PT-3",
  "mutations": [
    {
      "file": "apps/api/server/lib/ports/pg-decision-store.ts",
      "before": "              from decision where org_id = $1 and campaign_id = $2),",
      "after": "              from decision where $1::text is not null and campaign_id = $2), -- mutant: no org filter",
      "because": "without the org filter, one org reads another org's review decisions for a campaign id they share (D166: the org is the isolation boundary).",
      "command": [
        "yarn",
        "vitest",
        "run",
        "--project",
        "api",
        "apps/api/server/lib/ports/__tests__/pg-decision-store.test.ts",
        "-t",
        "another org"
      ],
      "verdict": "caught"
    }
  ]
}
