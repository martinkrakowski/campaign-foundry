// @generated by @hexagen/sync
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import deterministicCore from '../../eslint.deterministic-core.js';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  {
    rules: {
      '@typescript-eslint/no-explicit-any': 'warn',
      '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
    },
  },
  // Deterministic core, widened to the use cases. PipelineExecutionLog is the one
  // sanctioned wall-clock reader in the domain (it stamps the run log).
  ...deterministicCore({
    extraFiles: ['src/application/use-cases/**/*.ts'],
    ignores: ['src/domain/value-objects/PipelineExecutionLog.vo.ts'],
  }),
  {
    ignores: ['dist/**', 'node_modules/**'],
  },
);
